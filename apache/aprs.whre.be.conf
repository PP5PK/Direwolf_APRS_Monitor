<VirtualHost *:80>
     ServerName aprs.whre.be
     DocumentRoot /var/www/html
RewriteEngine on
RewriteCond %{SERVER_NAME} =aprs.whre.be
RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
</VirtualHost>
